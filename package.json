{
  "name": "voidrunners-space",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm --filter '*' run --parallel dev",
    "test": "pnpm --filter '*' run --parallel test"
  }
}
